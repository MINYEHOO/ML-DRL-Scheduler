# 2026-09-16 run 28 의도적 일시중지 (GPU 5 양보)

- 대상: `runs/28_narrow_mean_lr2000_s2002024_20260914_gpu5` (GPT 시드 캠페인, narrow_mean, seed 2002024, 목표 2000 updates).
- 이유: 사용자 지시로 보상 가중치 실험(`32_reward_c2m6_lr1000_s2024_20260916_gpu5`)을 GPU 5에서 먼저 실행. 컨테이너 사고가 아니라 **의도적 중지**.
- 방법: `ckpt/latest.pt` 갱신(mtime 변화)을 확인한 직후 trainer(pid 62088)에 SIGTERM → SIGKILL. 복구 supervisor(pid 4087, `recover_seed_replicates_20260915.py supervise`)는 자식 종료 후 스스로 종료했고 `jobs/28_*/status.json`은 `failed`로 기록됨(실패가 아닌 중지임을 이 문서로 명시).

| 항목 | 값 |
|---|---|
| 보존된 마지막 update (0부터) | 208 |
| `csv_logs/env_metrics.csv` 마지막 update | 208 (checkpoint와 일치, 잘린 행 없음 → CSV 수정 불필요) |
| optimizer lr | 2.688e-04 (= 3e-4 × (1 − 208/2000)) |
| lr_schedule | {'lr_final': 0.0, 'lr_decay_updates': 2000} |
| best.pt | 그대로 보존 |

## 재개 방법 (run 32 완료 후)

원본 checkpoint에서 native resume. 소스·recipe·LR 일정은 변경하지 않았으므로 protected trainer의 모든 가드를 그대로 통과한다.

`python -u paper_train_variants.py --recipe narrow_mean --resume runs/28_narrow_mean_lr2000_s2002024_20260914_gpu5/ckpt/latest.pt --device cuda --gpu 5 --threads 4`

(`--num-updates` 생략 시 manifest의 목표 2000이 복원된다. TensorBoard는 209부터 새 event가 이어지고, 과학적 분석은 CSV/checkpoint 경계를 기준으로 한다 — 9/15 복구 기록과 같은 원칙.)
